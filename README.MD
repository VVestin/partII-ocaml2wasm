# A compiler from OCaml to WebAssembly

To use the compiler, first install the required packages via npm:
```
$ npm install
```

Then simply run `src/main.js` from the project's root directory:
```
node src/main.js "2 + 2"
```
